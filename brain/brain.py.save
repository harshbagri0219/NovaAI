ion


class Brain:

    def think(self, user):
from brain.context import last_conversation


class Brain:

    def think(self, user):

        user = user.lower()

        history = last_conversation()

        if "what language did i just mention" in user:

            for item in reversed(history):

                text = item["user"].lower()

                if "language is" in text:

                    language = text.split("language is")[-1].strip()

                    return f"You just mentioned {language.title()}."

            return "You haven't told me a language yet."

        return "I am still learning."
        user = user.lower()

        history = last_conversation()
        if "what language did i just mention" in user:

            for item i    def think(self,que
